import { motion, AnimatePresence } from 'framer-motion';
import { FaCheck, FaEye } from 'react-icons/fa6';
import Button from './Button';
export default function RulesModal({open,onAccept,onClose}){return <AnimatePresence>{open && <motion.div className="modal-backdrop" onMouseDown={onClose} initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}}><motion.section className="rules-modal glass" role="dialog" aria-modal="true" aria-labelledby="rules-title" onMouseDown={e=>e.stopPropagation()} initial={{opacity:0,scale:.9,y:16}} animate={{opacity:1,scale:1,y:0}} exit={{opacity:0,scale:.96}}><div className="modal-icon"><FaEye /></div><p className="eyebrow">A mindful little challenge</p><h2 id="rules-title">How to play</h2><ul>{['Carefully observe the image pair.','Count the hidden differences.','Enter only the total number.','No time limit — take a calming pause.','One attempt per image. Try for a perfect score!'].map(x=><li key={x}><FaCheck />{x}</li>)}</ul><Button onClick={onAccept}>I’m ready to explore</Button></motion.section></motion.div>}</AnimatePresence>}
